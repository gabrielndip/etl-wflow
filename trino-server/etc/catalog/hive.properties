# Hive connector configuration (file metastore example)
connector.name=hive
hive.metastore=file
hive.metastore.catalog.dir=./trino-server/data/hive

# For a remote metastore, use:
# hive.metastore.uri=thrift://localhost:9083
